"""D367 whole-game asset pipeline (docs/D367_ASSET_PIPELINE.md).

Entry point: tools/d367/assets.sh (assets.py). Formula-driven, content-hash cached,
deterministic. Private inputs and outputs live under RE4DC_ASSETS_ROOT, never in Git.
"""
API = 1
